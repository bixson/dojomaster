apiVersion: v1
kind: Service
metadata:
  name: dojomaster
  labels:
    app: dojomaster
spec:
  selector:
    app: dojomaster
  ports:
    - protocol: TCP
      port: 80
      targetPort: 7681
  type: ClusterIP
